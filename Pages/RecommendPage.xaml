<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Provisio.Pages.RecommendPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:svc="using:Provisio.Services">

    <Grid Padding="36,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4" Margin="0,0,0,8">
            <TextBlock x:Name="TitleText" Text="Recommended Setup" svc:L.Key="rec.title" Style="{ThemeResource TitleLargeTextBlockStyle}" />
            <TextBlock x:Name="SubtitleText" TextWrapping="Wrap" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <StackPanel Grid.Row="1" x:Name="ProgressPanel" Orientation="Horizontal" Spacing="12" Margin="0,4,0,12" Visibility="Collapsed">
            <TextBlock x:Name="StepText" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <ProgressBar x:Name="StepProgress" Width="220" VerticalAlignment="Center" Minimum="0" Maximum="100" />
        </StackPanel>

        <ScrollViewer Grid.Row="2">
            <StackPanel x:Name="Host" Spacing="8" MaxWidth="820" HorizontalAlignment="Left" />
        </ScrollViewer>

        <Grid Grid.Row="3" Margin="0,20,0,0">
            <StackPanel Orientation="Horizontal" Spacing="12" HorizontalAlignment="Left">
                <Button x:Name="BackButton" Content="Back" svc:L.Key="common.back" Click="Back_Click" Visibility="Collapsed" />
                <Button x:Name="RestartButton" Content="Start over" svc:L.Key="rec.restart" Click="Restart_Click" Visibility="Collapsed" />
            </StackPanel>
            <Button
                x:Name="PrimaryButton"
                Content="Start questionnaire"
                Style="{ThemeResource AccentButtonStyle}"
                HorizontalAlignment="Right"
                Click="Primary_Click" />
        </Grid>
    </Grid>
</Page>
