<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Provisio.Pages.ProfilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:svc="using:Provisio.Services">

    <Grid Padding="36,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="4" Margin="0,0,0,16">
            <TextBlock Text="Profiles" svc:L.Key="profiles.title" Style="{ThemeResource TitleLargeTextBlockStyle}" />
            <TextBlock
                svc:L.Key="profiles.subtitle"
                TextWrapping="Wrap"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <ListView x:Name="ProfilesList" Grid.Row="1" />
    </Grid>
</Page>
